<div id="favorite-<%= @recipe.id %>">
  <% if !current_user.nil? && current_user.favorite?(@recipe) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>