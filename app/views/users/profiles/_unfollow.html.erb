<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "フォロー解除", class: "col-md-2 col-sm-4 btn btn-secondary ml-3 my-3" %>
<% end %>